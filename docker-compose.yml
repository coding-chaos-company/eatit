version: "3.0"

services:
  api:
    build:
      context: ./docker/api
      dockerfile: Dockerfile
    volumes:
      - ./api:/works/app:cached
      - ./scripts:/works/scripts:cached
    working_dir: /works/scripts
    command: bash -c "./run.sh"
    ports:
      - 8080:8080
